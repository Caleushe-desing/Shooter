/** Flat patio height — open-world mountains removed. */

export function sampleHeight(_x: number, _z: number): number {
  return 0
}

export function sampleSlope(_x: number, _z: number): number {
  return 0
}
